# Day 9 Exercise

## Task

Complete `exercise.py`.

## Requirements

Your script should:

- list files in a folder
- create a `Path` object
- write a small JSON file
- read a CSV file if one exists

## Stretch

- print file sizes

## Questions

When you finish, answer:

1. Why is `pathlib` often nicer than string paths?
2. When would you use CSV instead of JSON?
3. What does `os.listdir()` do?
