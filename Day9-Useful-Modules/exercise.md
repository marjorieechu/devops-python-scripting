# Day 9 Exercise

## Task

Complete `exercise.py`.

## Requirements

Your script should:

- list files in a folder
- create a `Path` object
- write a small JSON file
- read a CSV file if one exists

## Stretch

- print file sizes

## Questions

When you finish, answer:

1. Why is `pathlib` often nicer than string paths? Handles path in a cleaner way.
2. When would you use CSV instead of JSON? when the file in question is  csv and not a json file.
CSV is better when your data is tabular, simple, and needs to be shared with spreadsheets or data tools. JSON is better for nested or structured data
3. What does `os.listdir()` do? list names of files and folders of a directory.
