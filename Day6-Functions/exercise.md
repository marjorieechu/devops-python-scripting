# Day 6 Exercise

## Task

Edit `exercise.py` so it includes:

- a function to greet a user
- a function to add two numbers
- a function to check if a number is even

## Requirements

- call each function at least once
- print the result clearly

## Stretch

Add one more function called `format_report(name, total)` that returns a short report line.

## Questions

When you finish, answer:

1. What is the difference between `print` and `return`?
2. Why are functions useful in scripts?
3. What is a parameter?

1. print outputs values while return store value to be reusued later. print is for users while return is for logic.
2. Useful because they split or group codes into smaller pieces for easier maintance and read, promote reusability and reduce repitition.
3. A parameter is variable used inside of a function.
