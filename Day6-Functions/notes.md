# Day 6 Notes

## Mistakes and Fixes

- If I forget to call a function, nothing happens.
- If I use `print()` instead of `return`, I cannot reuse the result easily.
- A parameter is the variable written inside the function definition.

# My notes 
Function groups code into smaller piece for reusability
Parameter is the variable inside the function. Imput given to the function. Placeholder.
Argument is the actual value passed into the function.

A function can perform actions like print and return
Key Difference
| print()            | return              |
| ------------------ | ------------------- |
| Displays output    | Sends value back    |
| Can't reuse easily | Can store/use later |
| For users          | For logic           |

Calling the function means executing the function or running the function code with python.
A function can be defined to print or to return or to do neither actions to do both actions.
A function can also be defined without a parameter or an arguement and still return a value.
Arguements are only needed when your function depends on external imput.
A `return` does not necessarily need an arguement to send value.
A `function` with a ``return` can be reused multiple times, and each time you call it, you can pass different arguments (if it accepts parameters).


On exercise.py you dont need to write the function again to call the function.