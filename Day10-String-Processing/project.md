# Day 10 Mini-Project

## Task

Build a log parser in `project.py`.

## Requirements

Your script should:

- read lines from a text file
- count lines containing `"ERROR"`
- count lines containing `"INFO"`

## Hint

Loop through the lines and use `in` to check whether each keyword appears.
