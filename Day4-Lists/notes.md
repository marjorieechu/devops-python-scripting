# Day 4 Notes

Day: 4

What I learned:
- A list stores multiple values in one variable.
- Python list indexes start at 0.
- Negative indexes count from the end of the list.
- A slice returns part of a list.
- `.append()` adds one item to the end of a list.
- `.remove()` deletes a matching value from the list.
- A `for` loop can print each item in a list one by one.

What I built:
- A fruit list practice script and a task list manager

What errors I hit:
- None

How I fixed them:
- 

What still feels unclear:
- 

## Quick Check

Answer these in your own words:

1. What index is the first item in a list? `0`
2. What does `my_list[-1]` mean? The last item in the list.
3. What does `my_list[1:3]` return? The items from index `1` up to but not including index `3`.
