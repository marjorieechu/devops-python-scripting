# Day 2 Notes

Day: 2

What I learned:
- Conditions and decisions
- Indentation Rule: use of 4 spaces. inside the condition block, all codes lines must be indented
- use of : after every condition
- use of == rather than = within the code
- the use of "not" to reverse a boolean value

What I built:
- condition and decisiion statement

What errors I hit:
- None

How I fixed them:
- nil

What still feels unclear:
- nothing

## Quick Check

Answer these in your own words:

1. What is the difference between `if`, `elif`, and `else`? 
- `if` passes true conditions, `elif` introduces a second condition before the fall back case while `else` passes the fall back case.
2. What is the difference between `=` and `==`?
- `=` is valid in python. It is an assignment operator used store value in a variable while `==` is a comparison operator used to compare equality of two values.
3. Why does the order of conditions matter?
- To avoild condition overlapping and errors


