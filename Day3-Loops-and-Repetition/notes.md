# Day 3 Notes

Day: 3

What I learned:
- A for loop is a control structure used to repeat a block of code in sequence declared by a list or a range()
- A range is a built in function in python generally that generates a sequence numbers that a for loop can itertrate on.
- the numbers inside a loop are parameters.
- for the parameters in the range, the last number of the range is not included in the sequence. this is a rule.
- 5 means 0 to 4
- 1,6 means 1 to 5
- 2, 11, 2 means 2 to 10 moving in 2s
- A while loop will continue the sequence untill a condition becomes false.
- A count is a variable that counts the number of times an action occurs in a program. It is the variable that counts.
- Lines after every loop should be inted as well as lines after a condition.

What I built:
- A deployment timer script and server list with even number IDs

What errors I hit:
- None

How I fixed them:
- 

What still feels unclear:
- 

## Quick Check

Answer these in your own words:

1. What is the difference between a `for` loop and a `while` loop? For loop controls the sequence of repeatition but the   A while loop continues while its condition stays true. It becomes infinite only if the condition never becomes false
2. What does `range(5)` produce? 1, 2, 3, 4.
3. Why can a `while` loop become dangerous if you forget to update the counter? continues infinitely
