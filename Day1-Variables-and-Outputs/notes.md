# Day 1 Notes

Day: 1

What I learned:
- what variables, strings, numbers, f-strings and prints are.
use of quotes ", curly brackets(f-strings) and parenthesis(prints)

What I built:
- A profile card

What errors I hit:
- misplaced quotes and string spellings

How I fixed them:
- with the aid some vscode extensions, the mistakes were pointed out

What still feels unclear:
- Following gradually

## Quick Check

Answer these in your own words:

1. What is a variable? A variable is an item used to store values
2. What is the difference between a string and a number? A string is a text while a number is an arithmetic representation that can be calculated
3. Why use an f-string? Use an f string to call a variable inside a text
