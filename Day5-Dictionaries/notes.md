# Day 5 Notes

Day: 5

What I learned:
- A dictionary stores related data as key-value pairs.
- Keys are the names used to access values.
- `.get()` reads a value safely when a key might not exist.
- A nested dictionary stores another dictionary inside it.
- Dictionaries are useful when data has labels like `name`, `age`, or `city`.

What I built:
- A profile dictionary practice script and a server inventory mini-project

What errors I hit:
- None

How I fixed them:
- 

What still feels unclear:
- 

## Quick Check

Answer these in your own words:

1. Why would you use a dictionary instead of a list? A dictionary is better when each value needs a clear label or key.
2. What problem does `.get()` solve? It avoids an error when a key is missing.
3. What is a nested dictionary? A dictionary stored inside another dictionary.
